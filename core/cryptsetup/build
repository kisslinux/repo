#!/bin/sh -e

machine=$(gcc -dumpmachine)

./configure \
    --prefix=/usr \
    --build="$machine" \
    --host="$machine" \
    --disable-selinux \
    --enable-static-cryptsetup \
    --disable-blkid

make
make DESTDIR="$1" install
